<h1>Обзор статьи рубрики "Это интересно"</h1>
<?php if($this->messages): ?><div id="user_msg" class="success"><?php echo implode($this->messages); ?></div><?php endif;?>
    <section id="content_wrapper">
    	<p>Статья: <span class="bold"><?php echo $this->escape($this->article['title']); ?></span></p><br>
	    <?php if (count($this->blocks)): ?>
				<table id="prod_tbl">
		        	<thead>
		            	<tr>
		                	<td>Фото</td>
		                    <td>Описание</td>
		                    <td>Действия</td>
		                </tr>
		            </thead>
		            <tbody>
						<?php foreach ($this->blocks as $block): ?>
							<tr>
			                	<td><img class="master" src="/allarticles/<?php echo $this->escape($block['article_id']); ?>/<?php echo $this->escape($block['img']); ?>"></td>
			                	<td class="left_txt"><?php echo nl2br($this->escape($block['description'])); ?></td>
			                    <td>
			                    	<a href="<?php echo $this->url(array('id' => $this->escape($block['img_id'])), 'admin-articles-editblock', true); ?>">
			                    		<img src="/images/edit1.png" title="Редактировать блок"></a>
			                    	<?php if (count($this->blocks) > 1): ?>
			                    		<a href="<?php echo $this->url(array('id' => $this->escape($block['img_id'])), 'admin-articles-delblock', true); ?>">
			                        		<img src="/images/cancel(3).png" title="Удалить блок"></a>
			                        <?php endif; ?>
			                    </td>
			                </tr>
						<?php endforeach; ?>
					</tbody>
		        </table>
        	
		<?php else: ?>
			<p>Пока нет ни одной фотографии с описанием в данной статье</p>
		<?php endif; ?>
	</section>
	<section class="section_wrapper">
		<p>Добавить блок с фотографией к статье</p>
		<?php echo $this->form; ?>
	</section>